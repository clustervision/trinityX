<?xml version="1.0" encoding="utf-8"?>
<direct>
  {% for rule in firewalld_direct_rules %}
  <rule priority="{{ rule.priority }}" table="{{ rule.table }}" ipv="{{ rule.ipver }}" chain="{{ rule.chain }}">{{ rule.rule }}</rule>
  {% endfor %}
</direct>
