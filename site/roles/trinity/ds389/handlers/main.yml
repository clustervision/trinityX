---
# handler file for ds389

- name: restart ds389
  systemd:
    name: dirsrv@local
    state: restarted
  when: primary|default(True)

- name: create ds389 runtime dirs
  command: systemd-tmpfiles --create /etc/tmpfiles.d/dirsrv-local.conf
