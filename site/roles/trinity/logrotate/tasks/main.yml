---
- name: Copy logrotate configuration files
  copy:
    src: '{{ item }}'
    dest: '/etc/logrotate.d/{{ item }}'
    force: "no"
    owner: root
    group: root
  with_items:
    - trinityx
    - uchiwa
    - sensu
    - slurm
    - influxdb
    - telegraf
    - luna
