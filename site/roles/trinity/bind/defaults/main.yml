---
# vars file for bind

bind_packages:
  - bind
bind_dns_forwarders:
  - '8.8.8.8'
  - '8.8.4.4'
bind_dnssec_enable: 'no'
bind_db_path: '/var/named'

resolv_server: '127.0.0.1'
resolv_search_domains: 'cluster ipmi'
