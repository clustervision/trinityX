To expire projects the admin can either use [[sbank-deposit]] and remove
all the accounts from an account or use [[sbank-project]] to expire
an account.

	$ sbank project expire -c mycluster -a myaccount

The above command simply zero's the account limit.
