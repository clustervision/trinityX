---
- name: Reload and restart prometheus-amd-exporter
  listen: "restart prometheus-amd-exporter"
  become: true
  systemd:
    daemon_reload: true
    name: prometheus-amd-exporter
    state: restarted
  when: not in_image
