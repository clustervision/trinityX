---
# handler file for alertx

# placed here for possible future use, however we
# manage the start/stop from the tasks itself
- name: restart drainer
  service:
    name: alertx-drainer
    state: restarted
  when: on_controller
