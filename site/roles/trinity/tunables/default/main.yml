
# Recommended tuned profiles are hpc-compute, network-latency, throughput-performance
# To view the available profiles for your system, use 'tuned-adm list'

compute_tuned_profile: 'hpc-compute'
controller_tuned_profile: 'throughput-performance'

tunables_packages:
  - tuned
