---
# vars file for environment-modules

environment_modules_packages:
  - environment-modules
  - userspace-modulegroups-tr17.10

modulefiles_path: '{{ trix_shared }}/modules'
modulefiles_subdirs:
  - groups

